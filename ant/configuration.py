name = "ant"  # the name of the command we type in ANT
compile = ""  # if empty is not executed
execute = "python main.py"  # the string that ANT sends in bach to execute
arguments = []  # arguments that can be passed by default
