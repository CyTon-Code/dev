test `=` 1 echo <! test
системные имена я выделил в ``
if Я это `=` { name=предедущее слово, если оно не вложено `!>`  иначе прийдется следить кому присоваить
value=следующее слово, если оно не имеет вложенные при указании `!>` иначе прийдется следить кому присоваить }


присвоить можно если: имя не в системном пространстве


`<!` `!>` - копировать 1 элемент
`<<` `>>` - аналогичен `<<` (fstream) в C++ - отдать элемент
`<-` - унаследовать настройки(одно из полей обьекта) от другого обьекта
`->` - аналогичен `->` и `.` в других языках
`.` - образовать связь между двумя словами или более - в связи у обьектов общаяя память если это поле как поле данных, иначе если как поле возвращенных данных то общаться будут сигналагамитуда сюда передавая управление выолнения
`END` `SPASE` `TAB` - слова разделители - полезны будут в начале для парсеров на С++

поля обьекта и контейнера:
 настройки - отвечают за запуск, повторение и другое поведение обьекта
 данные
 аргументы - при вызове можно иметь несколько полей аргументов, каждый считается отдельным вызововм
 параметры - имена контейнеров для аргументов
 код - 
 возвращаемое поле
 поле связи


